{
  "name": "pi-state-machine",
  "private": true,
  "type": "module",
  "pi": {
    "extensions": [
      "./extensions/orchestrator.ts"
    ]
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
